"""Standalone marketplace core package.

This package is intentionally independent from the domain-specific agents in
`src/ai_agents`. External agents should integrate through the Registry API,
the SDK client, signed envelopes, and transport endpoints.
"""

